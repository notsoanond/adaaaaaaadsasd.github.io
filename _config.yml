### Site metadata

# Don't make the title longer than this, otherwise it gets covered in mobile view.
title: adaaaaaaadsasd's Blog
description: adaaaaaaadsasd's Blog

email:
twitter_username:
github_username: adaaaaaaadsasd
rss:

### Build settings

url: https://adaaaaaaadsasd.github.io
baseurl: /

permalink: pretty

theme: minima

plugins:
  - jekyll-sitemap

collections:

defaults:
  - scope:
      path: ""
    values:
      layout: page

exclude:
  - docs/
  - vendor/
  - Gemfile # Gemfile.lock is implied because of how matching works.
  - LICENSE
  - Makefile
  - README.md
  - sample*.png

liquid:
  error_mode: strict

strict_front_matter: true
